[build]
builder = "dockerfile"

[deploy]
startCommand = "php artisan serve --host=0.0.0.0"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[healthCheck]
enabled = true
interval = 10
timeout = 5
retries = 3
startPeriod = 30
